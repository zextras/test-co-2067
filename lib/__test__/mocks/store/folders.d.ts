import { PopulateFoldersStoreOptions } from '../../../types/folder';
/**
 * Initialize the folder's store with roots and folders provided by
 * the mocks generators
 */
export declare const populateFoldersStore: ({ view, noSharedAccounts, customFolders }?: PopulateFoldersStoreOptions) => void;
