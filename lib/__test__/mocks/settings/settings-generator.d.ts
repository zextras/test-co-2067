import { AccountSettings } from '@zextras/carbonio-shell-ui';
/**
 *
 * @param customSettings
 */
declare const generateSettings: (customSettings?: Partial<AccountSettings>) => AccountSettings;
export { generateSettings };
