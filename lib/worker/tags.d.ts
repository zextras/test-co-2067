import { SoapNotify } from '@zextras/carbonio-shell-ui';
import type { Tag, Tags } from '../types/tags';
export declare const handleTagRefresh: (tags: Array<Tag>) => Tags;
export declare const handleTagNotify: (notify: SoapNotify, state: Tags) => Tags;
