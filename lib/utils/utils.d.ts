import { Folder } from '../types/folder';
export declare const isValidFolderName: (folderName: string) => boolean;
export declare const allowedActionOnSharedAccount: (folder: Folder, action: string) => boolean;
